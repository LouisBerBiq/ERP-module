<odoo>
    <record id="view_product_rental_form" model="ir.ui.view">
        <field name="name">product.template.rental.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <field name="sale_ok" position="after">
                <field name="is_bike"/>
                <field name="rent_price_day" invisible="not is_bike"/>
            </field>
        </field>
    </record>

    <record id="view_bike_rental_form" model="ir.ui.view">
        <field name="name">bike.rental.form</field>
        <field name="model">bike.rental</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirmer" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_done" string="Retour Vélo" type="object" invisible="state != 'confirm'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title"><h1><field name="name"/></h1></div>
                    <group>
                        <group><field name="partner_id"/><field name="bike_id"/></group>
                        <group><field name="start_date"/><field name="end_date"/><field name="total_price"/></group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_bike_rental_tree" model="ir.ui.view">
        <field name="name">bike.rental.tree</field>
        <field name="model">bike.rental</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="bike_id"/>
                <field name="total_price"/>
                <field name="state"/>
            </list>
        </field>
    </record>
</odoo>